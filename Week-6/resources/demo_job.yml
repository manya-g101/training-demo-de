resources:
  jobs:
    week6_demo_job:
      name: week6-demo-job
      tasks:
        - task_key: demo-task
          notebook_task:
            notebook_path: ${var.demo_notebook_path}
            source: WORKSPACE
